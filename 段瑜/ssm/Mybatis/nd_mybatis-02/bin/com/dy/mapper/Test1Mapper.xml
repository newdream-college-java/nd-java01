<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dy.mapper.Test1Mapper">
	 <insert id="save" parameterType="com.dy.entiy.Test1">
		<selectKey keyProperty="orderid" order="BEFORE" resultType="java.lang.String">
			select uuid()
		</selectKey>
		insert into test1(orderid,number) values(#{orderid},#{number})
	</insert>
	<select id="findAll" resultType="Test1">
	</select>
</mapper>