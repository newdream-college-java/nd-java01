<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.wx.mapper.ItemsMapper">
  	<!--  items商品表>>>>订单详情ordersdetail:一对多 -->
  	<!-- Items findOrderdetail(int a); -->
  	<resultMap type="Items" id="ItemsByOrderdetail">
  		<result property="name" column="items_name" />
		<result property="detail" column="items_detail" />
			<collection property="orderdetail" ofType="Orderdetail">
				<result property="id" column="orderdetail_id" />
				<result property="itemsId" column="items_id" />
				<result property="itemsNum" column="items_num" />
			</collection>
  	</resultMap>
  	<select id="findOrderdetail" resultMap="ItemsByOrderdetail">
  		SELECT
		orderdetail.id orderdetail_id,
		orderdetail.items_num,
		orderdetail.items_id,
		items.name items_name,
		items.detail items_detail
		FROM
		orderdetail,
		items
		WHERE 
		items.id = orderdetail.items_id
		and items.id=#{itemsId}
  		
  	</select>
  </mapper>