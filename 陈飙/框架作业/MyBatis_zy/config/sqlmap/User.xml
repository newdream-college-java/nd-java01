<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newdream">
	<!-- 查询 -->
	<select id="findALLUser" resultType="com.cb.entity.User">
		select id,username, birthday,sex,address from user
  </select>
	<!-- 模糊查询 -->
	<select id="findByName" parameterType="java.lang.String"
		resultType="com.cb.entity.User">
		select id, username,birthday,sex,address from user where username like
		'%${value}%'
  </select>
	<!-- 模糊单个 -->
	<select id="findById" parameterType="int" resultType="com.cb.entity.User">
		select id, username,birthday,sex,address from user where id =#{id}
  </select>
	<!--添加 -->
	<insert id="addUser" parameterType="com.cb.entity.User">
		insert into user
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="username!=null">
				username,
  			 </if>
  			 <if test="birthday!=null">
				birthday,
  			 </if>
  			 <if test="sex!=null">
				sex,
  			 </if>
  			 <if test="address!=null">
				address,
  			 </if>
		</trim>
		values
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="username!=null">
			#{username},
			</if>
			<if test="birthday!=null">
			#{birthday},
			</if>
			<if test="sex!=null">
			#{sex},
			</if>
			<if test="address!=null">
			#{address},
			</if>
		</trim>
	</insert>
</mapper>